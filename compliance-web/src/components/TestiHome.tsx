"use client"

type TestimProps = {
    testim_title: string;
    testim_subTitle: string;
};

export default function TestimHome( { testim_title, testim_subTitle } : TestimProps) {
    return(
        <>
            
        </>
    );
}